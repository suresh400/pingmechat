{
  "name": "pingme-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd pingme-frontend && npm install && npm run build",
    "start": "cd pingme-frontend && npm start"
  }
}
